# Development Scripts

This directory contains development and deployment utilities.

Planned scripts include:

* environment initialization
* genesis initialization
* node startup
* testnet deployment
* contract deployment
* RPC health checks
* network diagnostics

Scripts must never contain private keys, passwords or production credentials.
